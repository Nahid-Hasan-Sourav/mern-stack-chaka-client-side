import { useQuery } from '@tanstack/react-query';
import React from 'react';

const Myorders = () => {
   
    return (
        <div>
            My Oredrs pages
        </div>
    );
};

export default Myorders;